import 'package:flutter/material.dart';

class AsiaView extends StatelessWidget {
  const AsiaView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Asia"),
      ),
    );
  }
}
