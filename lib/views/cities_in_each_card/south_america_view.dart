import 'package:flutter/material.dart';

class SouthAmericaView extends StatelessWidget {
  const SouthAmericaView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("South America"),
      ),
    );
  }
}
