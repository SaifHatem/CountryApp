import 'package:flutter/material.dart';

class NorthAmericaView extends StatelessWidget {
  const NorthAmericaView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("North America"),
      ),
    );
  }
}
