import 'package:flutter/material.dart';

class AustraliaView extends StatelessWidget {
  const AustraliaView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Australia"),
      ),
    );
  }
}
